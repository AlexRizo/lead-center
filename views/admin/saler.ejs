<div class="section lead">
    <div class="cards-content">
        <div class="principal-info">
            <div class="card principal user-principal">
                <img class="profile-img" src="\assets\img\profile.png" alt="profile picture" width="100px">
                <h4 class="username"><%= user.name %></h4>
                <div class="details">
                    <div class="pending-leads-count">
                        <nav class="status-title">Pendientes</nav>
                        <nav><%= pending %></nav>
                    </div>
                    <div class="pending-leads-count">
                        <nav class="status-title">En Seguimiento</nav>
                        <nav><%= follow %></nav>        
                    </div>
                    <div class="pending-leads-count">
                        <nav class="status-title">Contactados</nav>
                        <nav><%= contacted %></nav>
                    </div>
                </div>
            </div>
            <div class="card saler">
                <div class="saler-title">
                    <h4>Nota del vendedor:</h4>
                    <i class="fa-solid fa-pen-to-square pointer edit-saler-note" style="color: #000000;"></i>
                </div>
            </div>
        </div>
        <form class="card user-data">
            <div class="card-header">
                <h3 class="card-title">Información del Vendedor</h3>
            </div>
            <div class="card-body">
                <input name="id" type="hidden" id="id" value="<%= user.id %>"/>
                <div class="inputs-content">
                    <div class="input-content">
                        <label for="name">Nombre Completo</label>
                        <input name="name" class="input input-form" type="text" id="name" maxlength="100" required placeholder="Nombre del Vendedor" value="<%= user.name %>"/>
                    </div>
                    <div class="input-content">
                        <label for="email">Correo</label>
                        <input name="email" class="input input-form" type="email" id="email" maxlength="100" required placeholder="Correo Electrónico" value="<%= user.email %>"/>
                    </div>
                </div>
                <div class="input-content">
                    <label for="password">Contraseña</label>
                    <input name="password" class="input input-form" type="password" id="password" maxlength="100" placeholder="Contraseña"/>
                </div>
                <div class="inputs-content">
                    <div class="input-content">
                        <label for="roleId">Rol</label>
                        <select name="roleId" id="roleId" class="input input-form" required>
                            <% if (user.roleId === 1) { %>
                                <option selected value="1">Vendedor</option>
                                <option value="2">Gerente</option>
                            <% } else if (user.roleId === 2) { %>
                                <option value="1">Vendedor</option>
                                <option selected value="2">Gerente</option>
                            <% } else { %>
                                <option value="1">Vendedor</option>
                                <option value="2">Gerente</option>
                                <option selected value="3">Administrador</option>
                            <% } %>
                        </select>
                    </div>
                    <div class="input-content">
                        <label for="status">Estado</label>
                        <select name="status" id="status" class="input input-form" required>
                            <% if (user.status) { %>
                                <option selected value="1">Activo</option>
                                <option value="0">Inactivo</option>
                            <% } else { %>
                                <option value="1">Activo</option>
                                <option selected value="0">Inactivo</option>
                            <% } %>
                        </select>
                    </div>
                </div>
            </div>
            <div class="card-footer">
                <button class="btn btn-form submit" type="submit">Guardar Cambios</button>
            </div>
        </form>
    </div>
</div>

<script src="/js/admin/saler.js"></script>